Django==5.1.6
djangorestframework==3.15.2
djangorestframework-simplejwt==5.5.0
django-cors-headers==4.6.0
dj-database-url==2.3.0
psycopg[binary]==3.2.4
python-dotenv==1.0.1
pymupdf>=1.24.0
groq>=0.9.0
python-docx>=1.1.0
# Local audio transcription (runs Whisper on this server, no external API).
# Requires system `ffmpeg` to be installed for audio decoding.
faster-whisper>=1.0.3
# Branded PDF generation + merging + QR verification codes.
reportlab>=4.0
pypdf>=4.0
qrcode[pil]>=7.4
# Used to instance the variable-weight Mulish font (the only form Google
# Fonts ships) into static TTFs at the weights reportlab needs.
fonttools>=4.50
brotli>=1.1
# AWS SES email backend (uses boto3 with IAM credentials — same access key
# we already use for S3, no separate SES SMTP credentials needed).
django-ses>=4.0
boto3>=1.34
